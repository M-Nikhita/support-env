[project]
name = "support-env"
version = "1.0.0"
description = "Customer Support OpenEnv Environment"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
